#!/bin/bash

#user management script

read -p 'Enter your username?: ' user
if [ -d  /home/$user ]
then 
        echo 'you cannot duplicate users'
       echo 'User already exist, try again next time'
else 
        sudo useradd $user
     echo user $user has been created successfully
fi

#.....................................................................................................


#ken's task
#create your bash script to delete users, you can also improve on this one